# Documentation Immo Dary

Documentation interne (journaux, rapports PFA, captures) — non versionnée dans Git.

Le code source se trouve dans :

- `backend/` — API REST
- `mobile/` — application Android
- `database/` — scripts SQL
- `deploy/` — Docker et déploiement
